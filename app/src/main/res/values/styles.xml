<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light">
        <item name="android:actionBarStyle">@style/OrangeActionBar</item>
    </style>

    <style name="AppTheme.NoTitleBar" parent="@android:style/Theme.NoTitleBar">
        <item name="android:windowBackground">@color/orange_normal</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/white</item>
    </style>

    <style name="AppTheme.OrangeBackground">
        <item name="android:windowBackground">@color/orange_dark</item>
    </style>

    <style name="OrangeActionBar" parent="@android:style/Widget.Holo.Light.ActionBar.Solid.Inverse">
        <item name="android:background">@color/orange_normal</item>
    </style>

</resources>
